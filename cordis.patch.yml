# dsh-mcp-skill-control bundle 层：插入 MCP 管理面板 host 行。
# 浏览器半不进 patch——package.json 的 dsh.client 声明由 client-modules
# 节点半从 loader 树扫描自动进 window.__DSH_BOOT__。
- insert:
    - id: mcp-manager
      name: '@dsh-external/dsh-mcp-skill-control'
      config: {}
